<style scoped>
/* custom styles for this example */

#default_lang_output, #single_lang_output {
    font-style:italic;
}

#event_output {
    margin-left:1em;
    color:#00aa00;
    font-style:italic;
}

#demo .output {
    margin-bottom:1em;
    padding:10px;
    border:1px solid #D9D9D9;
}

#demo_output.rtl {
    text-align:right;
}

#switchlang   {
    margin-top:3em;
}
</style>

<div class="intro">
  <p>
     The "date-format-advanced" module of the DataType Utility introduces several standard date patterns.
     This helps bring consistency across pages when displaying dates.
  </p>
</div>

<div class="example yui3-skin-sam">
    {{>datatype-dateformat-advanced-source}}
</div>

<h2>Using Date Formatting</h2>

<p>To use the advanced Date formatting in your application, all you need to do is use the `datatype-date-format-advanced` module, and specify the (supported) language you need for the instance.</p>

```

// Default (no language specified)
YUI().use("datatype-date-format-advanced", function (Y) {
    Y.one("#default_lang_output").setHTML(
        Y.Date.format(new Date(), {
		      dateFormat: "WYMD_LONG",
		      timeFormat: "HM_SHORT",
		      timezoneFormat: "Z_SHORT"
		}));
    );
});

// Format Date For fr-FR
YUI({lang:"fr-FR"}).use("datatype-date-format-advanced", function (Y) {
    Y.one("#single_lang_output").setHTML(
        Y.Date.format(new Date(), {
		      dateFormat: "WYMD_LONG",
		      timeFormat: "HM_SHORT",
		      timezoneFormat: "Z_SHORT"
		}));
    );
});

```
